# نظام الإشعارات - TrainUp

## نظرة عامة

تم إضافة نظام إشعارات شامل يعمل على جميع صفحات المشروع المصادق عليها (الطلاب، الشركات، المشرفين).

## الملفات الرئيسية

### 1. `js/notifications-handler.js`
الملف المسؤول عن إدارة الإشعارات على مستوى النظام بأكمله.

## الميزات الرئيسية

### ✅ الفحص التلقائي للإشعارات
- يتم فحص الإشعارات الجديدة كل **30 ثانية**
- يعمل تلقائياً في الخلفية على جميع الصفحات المصادق عليها

### 🔔 شارات الإشعارات (Badges)
- **شارة الشريط الجانبي**: تظهر بجانب رابط الإشعارات في القائمة الجانبية
- **شارة الهيدر**: تظهر على أيقونة الإشعارات في رأس الصفحة (إن وجدت)
- **عنوان الصفحة**: يتم تحديث عنوان الصفحة ليعرض عدد الإشعارات غير المقروءة (مثل: `(3) Dashboard - TrainUp`)

### 🎨 التنبيهات المنبثقة (Toast Notifications)
عند وصول إشعار جديد:
- يظهر toast منبثق في أعلى يمين الشاشة
- يحتوي على عنوان ورسالة الإشعار
- يختفي تلقائياً بعد 6 ثوانٍ
- يمكن إغلاقه يدوياً بالضغط على زر X
- يمكن النقر عليه للانتقال إلى صفحة الإشعارات

### 🎵 الصوت (اختياري)
- يتم تشغيل صوت عند وصول إشعار جديد (يمكن تعطيله من الإعدادات)

### 🎭 التأثيرات البصرية
- **تأثير Pulse**: تنبض الشارات للفت الانتباه
- **انزلاق سلس**: Toast يظهر بحركة انزلاق سلسة من اليمين
- **تصميم متجاوب**: يعمل على جميع أحجام الشاشات

## أنواع الإشعارات المدعومة

```javascript
{
    'APPLICATION_UPDATE': 'fas fa-file-alt',      // تحديث على الطلب
    'APPLICATION_ACCEPTED': 'fas fa-check-circle', // قبول الطلب
    'APPLICATION_REJECTED': 'fas fa-times-circle', // رفض الطلب
    'NEW_MESSAGE': 'fas fa-envelope',             // رسالة جديدة
    'INTERNSHIP_POSTED': 'fas fa-briefcase',      // تدريب جديد
    'EVALUATION_REQUEST': 'fas fa-star',          // طلب تقييم
    'DEADLINE_REMINDER': 'fas fa-clock',          // تذكير بموعد
    'SYSTEM': 'fas fa-info-circle'                // إشعار نظام
}
```

## واجهات برمجة التطبيقات المستخدمة

### 1. فحص عدد الإشعارات غير المقروءة
```http
GET /notifications/unread-count
```
**الاستجابة المتوقعة:**
```json
{
    "success": true,
    "data": {
        "count": 5
    }
}
```

### 2. جلب الإشعارات الحديثة
```http
GET /notifications/recent?limit=5
```
**الاستجابة المتوقعة:**
```json
{
    "success": true,
    "data": [
        {
            "id": "123",
            "title": "Application Accepted",
            "message": "Your application for Software Intern has been accepted!",
            "type": "APPLICATION_ACCEPTED",
            "read": false,
            "createdAt": "2025-01-05T10:30:00Z"
        }
    ]
}
```

### 3. تعليم إشعار كمقروء
```http
PUT /notifications/{id}/read
```

### 4. تعليم جميع الإشعارات كمقروءة
```http
PUT /notifications/mark-all-read
```

## الدوال المتاحة

### `initializeNotifications()`
تهيئة نظام الإشعارات - يتم استدعاؤها تلقائياً عند تحميل الصفحة.

### `checkNotifications()`
فحص الإشعارات الجديدة يدوياً.

### `markNotificationAsRead(notificationId)`
تعليم إشعار محدد كمقروء.

### `markAllNotificationsAsRead()`
تعليم جميع الإشعارات كمقروءة.

### `showNotificationToast(notification)`
عرض toast لإشعار محدد.

```javascript
// مثال
showNotificationToast({
    title: 'New Application',
    message: 'You have received a new application',
    type: 'APPLICATION_UPDATE',
    createdAt: new Date().toISOString()
});
```

## التخصيص

### تعطيل الصوت
```javascript
localStorage.setItem('notificationSound', 'false');
```

### تغيير فترة الفحص
افتح `js/notifications-handler.js` وعدل السطر:
```javascript
notificationCheckInterval = setInterval(checkNotifications, 30000); // 30 ثانية
```

### تغيير مدة ظهور Toast
افتح `js/notifications-handler.js` وعدل السطر:
```javascript
setTimeout(() => {
    toast.style.animation = 'slideOutRight 0.3s ease-out';
    setTimeout(() => toast.remove(), 300);
}, 6000); // 6 ثوانٍ
```

## الصفحات التي تم إضافة نظام الإشعارات إليها

### صفحات الطلاب
- ✅ dashboard.html
- ✅ internships.html
- ✅ my-applications.html
- ✅ profile.html
- ✅ edit-profile.html
- ✅ evaluations.html
- ✅ notifications.html
- ✅ internship-details.html

### صفحات الشركات
- ✅ dashboard.html
- ✅ applications.html
- ✅ create-internship.html

### صفحات المشرفين
- ✅ supervisor-dashboard.html
- ✅ supervisor-companies.html
- ✅ supervisor-internships.html
- ✅ supervisor-students.html

## كيفية الاستخدام في صفحة جديدة

1. أضف السكريبت قبل سكريبت الصفحة الخاص:
```html
<script src="../../js/config.js"></script>
<script src="../../js/auth.js"></script>
<script src="../../js/api.js"></script>
<script src="../../js/notifications-handler.js"></script>
<script src="../../js/your-page.js"></script>
```

2. تأكد من وجود عنصر لرابط الإشعارات في الشريط الجانبي:
```html
<a href="notifications.html" class="nav-item">
    <i class="fas fa-bell"></i>
    <span>Notifications</span>
</a>
```

3. (اختياري) أضف أيقونة إشعارات في الهيدر:
```html
<div class="header-notifications" data-notification-icon>
    <i class="fas fa-bell"></i>
</div>
```

## الأحداث المخصصة

يمكنك الاستماع لحدث تحديث الإشعارات:
```javascript
window.addEventListener('notificationsUpdated', (event) => {
    const notifications = event.detail.notifications;
    console.log('New notifications:', notifications);
    // قم بمعالجة الإشعارات حسب احتياجك
});
```

## الاستكشاف والإصلاح

### الإشعارات لا تظهر
1. تحقق من أن المستخدم مسجل دخول (يتم فحص `isLoggedIn()`)
2. تحقق من عمل API endpoints
3. افتح Console وابحث عن أخطاء JavaScript

### الشارات لا تظهر
تحقق من أن عنصر رابط الإشعارات موجود في HTML:
```javascript
document.querySelector('a[href*="notifications.html"]')
```

### Toast لا يظهر
تحقق من أن CSS animations تم تحميلها بشكل صحيح.

## الأداء

- **استهلاك الموارد**: منخفض جداً
- **حجم الملف**: ~15 KB (غير مضغوط)
- **عدد الطلبات**: طلب واحد كل 30 ثانية فقط
- **التأثير على الأداء**: لا يؤثر على سرعة الصفحة

## المتطلبات

- Font Awesome (للأيقونات)
- `auth.js` (لفحص تسجيل الدخول)
- `api.js` (لإرسال الطلبات)
- متغيرات CSS (في `style.css`)

## الترخيص

جزء من مشروع TrainUp.
